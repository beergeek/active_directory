{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "A short description of this task",
  "parameters": {
    "admin_password": {
      "description": "The password of the administrator. Also used as the safe password",
      "type": "String[1]"
    },
    "domain": {
      "description": "Domain name to use for the new forest. Will be made lowercase",
      "type": "String[1]"
    },
    "allow_reboot": {
      "description": "Boolean to determine if rebooting the node is allowed",
      "type": "Boolean"
    },
    "install_dns": {
      "description": "Boolean to determine if DNS server is installed",
      "type": "Boolean"
    }
  }
}
